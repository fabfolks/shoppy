<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_session_path(resource_name), :method => :delete) %>        
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_session_path(resource_name))  %>  
  </li>
<% end %>
